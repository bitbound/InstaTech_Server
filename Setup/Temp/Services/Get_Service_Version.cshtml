@{
    if (File.Exists(Server.MapPath(InstaTech.App_Code.Config.Current.RC_Download_Paths["Windows Service"])))
    {
        var version = System.Diagnostics.FileVersionInfo.GetVersionInfo(Server.MapPath(InstaTech.App_Code.Config.Current.RC_Download_Paths["Windows Service"])).FileVersion.ToString();
        Response.Write(version);
    }
    else
    {
        Response.Write("0.0.0.0");
    }
}