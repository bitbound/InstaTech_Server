@{
    if (File.Exists(Server.MapPath("/Downloads/InstaTech_Client.exe")))
    {
        var version = System.Diagnostics.FileVersionInfo.GetVersionInfo(Server.MapPath("/Downloads/InstaTech_Client.exe")).FileVersion.ToString();
        Response.Write(version);
    }
    else
    {
        Response.Write("0.0.0.0");
    }
}